name: Required Failsafe

on:
  push:
    branches: [main]
    paths:
      - README.md
      - CHANGELOG.md
  pull_request:
    branches: [main]
    paths:
      - README.md
      - CHANGELOG.md

jobs:
  required-reusable:
    uses: ./.github/workflows/required_reusable.yaml